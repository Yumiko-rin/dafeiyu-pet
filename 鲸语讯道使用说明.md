# 鲸语讯道 · 大肥鱼桌宠 AI 对话面板使用说明

> 一只叫「大肥鱼」的鲸鱼娘，住进你的桌面，会聊天、会在气泡里碎碎念。
> 鲸语讯道就是她专属的「深海通讯频道」——深色玻璃卡 + 实色对话气泡，让对话文字清清楚楚。

---

## 1. 面板是什么

**鲸语讯道**（`pet/panels.py` → `DeepChatPanel`）是桌宠内置的 AI 对话窗口，由 DeepSeek 大模型驱动。它有这些特点：

- **不抢戏**：330×430 的小型深色玻璃卡，停在桌宠旁边不挡视野。
- **看得清**：微软雅黑 UI、14px、实色可读气泡，对话文字始终清晰锐利。
- **不打扰**：始终置顶但不抢焦点；打字时显示「正在码字」动效，回完自动停。

> 它不替代正经 AI 客户端——它是一只能陪你聊两句的桌宠。回复走鲸鱼娘人设（傲娇、嘴甜、叫主人「鱼片」），单轮 80 字以内，适合摸鱼和逗猫。

---

## 2. 怎么打开

两种方式，二选一：

| 方式 | 操作 |
| --- | --- |
| **双击桌宠** | 在桌宠身上（透明窗体）快速双击鼠标左键，面板会自动定位在桌宠**头顶、水平居中**处弹出。 |
| **右键菜单** | 右键桌宠 → 选择 `AI 对话（双击我）`。 |

弹出时会带 180ms 的淡入动画，关闭直接点右上角 `✕` 或按 `Esc`。

> **对话框始终跟随桌宠**：无论你拖动桌宠，还是它自己散步走动（或气泡出现导致窗体变大被夹回屏幕内），面板都会实时贴着它的头顶移动；若头顶上方放不下，会自动翻到脚下，并始终夹在屏幕范围内。

---

## 3. 界面布局

```
┌──────────────────────────────────────┐
│ 🐳  鲸语讯道                  ●  ✕   │   ← 头部：头像 + 标题 + 在线点 + 关闭
│     大肥鱼在线 · DeepSeek 已接通      │
├──────────────────────────────────────┤
│                                      │
│  ┌──────────────────────────────┐    │
│  │ 主人好，我是大肥鱼🐳…         │    │   ← 助手气泡（左侧，实色中紫 + 纯白文字）
│  └──────────────────────────────┘    │
│                       ┌────────────┐ │
│                       │ 主人好     │ │   ← 用户气泡（右侧，蓝紫渐变 + 纯白文字）
│                       └────────────┘ │
│                                      │
│  大肥鱼正在码字…                     │   ← 打字动效（深紫小字 + 三个点呼吸）
│                                      │
├──────────────────────────────────────┤
│  [ 给大肥鱼发消息… (Enter 发送) ]  ➤ │   ← 输入行：pill 形输入 + 圆形发送
└──────────────────────────────────────┘
```

| 区域 | 说明 |
| --- | --- |
| **头部** | 🐳 头像 + 标题「鲸语讯道」+ 副标题状态 + 绿色在线点（●）+ 红色悬浮关闭钮（✕） |
| **消息流** | 自动滚动到底；新增消息追加、旧的往上推。最多保留 40 轮历史上下文。 |
| **输入行** | pill 形 `QLineEdit`，提示「给大肥鱼发消息…（Enter 发送）」；右侧蓝紫圆形 ➤ 发送钮。 |

---

## 4. 怎么用

### 4.1 发消息

- 在底部输入框打字，**回车（Enter）即发**。
- 点击右侧 ➤ 发送钮也行。
- 发送时立刻在右侧追加你的气泡，助手气泡上方出现「正在码字…」，回完自动停。

### 4.2 看历史

- 消息流自动滚动到最新那条；想看旧的用鼠标滚轮往上滚。
- **气泡文字可复制**：用鼠标选中对话文字 → `Ctrl+C`（气泡开启了 `TextSelectableByMouse`）。

### 4.3 关闭面板

- 点右上角 `✕`，或
- 按键盘 `Esc`，或
- 直接右键桌宠选其他菜单项（面板会自动隐藏，不会销毁，下次打开还是同一个实例）。

### 4.4 三种气泡长什么样

| 角色 | 位置 | 配色 | 用途 |
| --- | --- | --- | --- |
| **用户气泡** (`user`) | 右侧 | 蓝紫渐变 `#6f6cff→#4f8bff` + 纯白文字 + medium 字重 | 你说的话 |
| **助手气泡** (`assistant`) | 左侧 | 实色 `#2e2c4d` 中紫 + 紫色描边 + **纯白文字** + 微软雅黑 14px | 大肥鱼的回复 |
| **错误气泡** (`err`) | 左侧 | 与助手气泡**同款实色基底**（`#2e2c4d` 中紫 + 纯白文字 + 微软雅黑 14px medium）+ **红色描边** + 文字前自动加 `⚠` 前缀 | 网络/Key 出错提示 |

> 三种气泡里，**用户气泡（蓝紫渐变）** 与 **助手/错误气泡（实色 `#2e2c4d`）** 两套基底统一、文字均为纯白微软雅黑，仅通过描边颜色（紫=正常 / 红=错误）区分，确保每条消息都清晰可读。

### 4.5 配置 API（任意 OpenAI 兼容 /v1 端点）

鲸语讯道不再绑定 DeepSeek，可接入**任意兼容 OpenAI `/v1` 的服务**：DeepSeek、OpenAI、OpenRouter、本地 Ollama / LM Studio / vLLM 等。首次发消息若没配 Key，会弹出红色错误气泡提示去设置。

**推荐（可视化面板，无需重启）：**
1. 右键桌宠 → **设置…**，打开设置面板。
2. **API 网址**填你的 `/v1` 基址，例如 `https://api.deepseek.com/v1`；OpenAI 填 `https://api.openai.com/v1`；本地模型填 `http://localhost:11434/v1` 之类。
3. **API Key** 粘贴对应服务商的 Key（密码形式隐藏，可勾「显示」查看）。
4. **模型**：点「刷新列表」会自动从 `{网址}/models` 拉取可用模型填进下拉框，直接选即可；也可手动输入（如 `deepseek-chat`、`gpt-4o`）。
5. 点「保存并应用」即可，**立刻生效**，不用重启。

**或手改配置文件：** 编辑 `dist/pet_config.json`（源码运行时是项目根的 `pet_config.json`），写入 `"api_key"`、`"api_base"`、`"model"`，保存后重启桌宠生效。

> 配置明文保存在 `pet_config.json`，公共电脑请勿勾选「记住」。
> 「查看模型余额」仅 DeepSeek 可用（调 `/user/balance`）；其它端点该项会提示查询失败属正常。

---

## 5. 视觉设计细节

| 维度 | 选择 | 原因 |
| --- | --- | --- |
| 整体卡片 | 深紫渐变 `rgba(38,36,66,0.96)→rgba(24,22,48,0.96)` + 紫色描边 + 36px 模糊投影 | 跟桌宠深色玻璃风格统一，不抢桌面焦点 |
| 中文字体 | `"Microsoft YaHei UI", "Microsoft YaHei", "PingFang SC", "Source Han Sans SC"` 优先链 | Windows 上中文最锐利清晰的 UI 字体，避免回退到细体衬线发虚 |
| 气泡字号 | 14px（中英文通用） | 兼顾可读性 + 紧凑布局（330px 宽面板） |
| 助手气泡 | 实色中紫 `#2e2c4d` + 纯白 `#ffffff` 文字 + medium 字重 | 在深紫卡片上气泡明显成块、纯白文字对比度拉满 |
| 用户气泡 | 蓝紫渐变 + 纯白文字 + medium 字重 | 区分发送方，渐变更有"己方"仪式感 |
| 圆角 | 14px（气泡）/ 16-18px（卡片/输入框） | 现代感 + 柔和 |
| 动效 | 弹出 180ms 淡入 / 打字 420ms 切换三点 / 气泡直接追加无位移动画 | 流畅但克制，不打扰工作 |

---

## 6. 常见问题

**Q1：面板打不开？**
- 桌宠本身没启动 → 先启动 `大肥鱼桌宠.exe`。
- 双击没反应 → 桌宠默认是单击说话、双击打开面板；两次点击间隔 < 280ms 才会被识别为双击。

**Q2：发了消息一直「正在码字」不返回？**
- 没配 API Key：先右键「设置…」填 Key。
- 网络/防火墙：DeepSeek API (`https://api.deepseek.com`) 走 HTTPS 出站。
- 余额不足：右键查余额。

**Q3：文字看着还是发虚/糊？**
- Windows 系统字体缺失会导致中文回退到衬线发虚。可手动确认「微软雅黑 UI」或「微软雅黑」在系统字体列表中（`C:\Windows\Fonts\msyh.ttc` / `msyhui.ttc`）。
- 高 DPI 缩放：把桌面缩放调到 100% 或 125% 都能保持锐利；>200% 时可能略有模糊。

**Q4：面板遮挡了桌宠怎么办？**
- 关闭旧实例后重新打开，面板会自动避开桌宠核心区域（算法：定位在桌宠右上角偏移处，溢出屏幕会自动裁剪）。

**Q5：聊天记录能保存吗？**
- 能。对话历史会实时写入 `chat_history.jsonl`（与 `pet_config.json` 同目录），**重启桌宠后自动回放**，不丢上下文。
- 仅保留最近 **40 轮**（超出的旧记录自动裁剪），避免文件无限膨胀；文件缺失/损坏也不影响启动。
- 想长期存档某段对话，仍可用气泡「文字可选中」功能手动复制。

**Q6：有音效吗？**
- 有，且很克制：单击桌宠蹦跳时来一声轻「啵」，收到大肥鱼回复时来一声轻「叮」。
- 音效文件是随包自带的 `sounds/pop.wav`、`sounds/ding.wav`（纯合成、无外部依赖）。
- 不想要可在右键「设置…」里关掉「音效」开关，或系统没音频设备时会自动静默跳过。

**Q7：它会主动找我聊天吗？**
- 会一点。空闲时（没开对话、没拖拽）它会**随机戳一戳**你（"戳戳主人～在忙什么呀？"），或**定时自发碎碎念**一句陪伴闲聊，增强陪伴感；你一互动它就安静下来陪你。

---

## 7. 相关文件

| 文件 | 作用 |
| --- | --- |
| `pet/panels.py` | 「鲸语讯道」面板实现（`DeepChatPanel`） |
| `pet/services.py` | DeepSeek 对话/余额网络请求 + 后台线程 + 历史持久化 |
| `pet/sound.py` | 音效管理（`SoundManager`，QSoundEffect，缺失静默跳过） |
| `pet/settings.py` | 可视化设置面板（`SettingsDialog`） |
| `pet/pet_window.py` | 桌宠主窗口：双击触发 `open_chat`、tick 消费 `("chat", role, text)`、摸鱼氛围定时器 |
| `pet/lines.py` | 鲸鱼娘台词库（短句 / 碎碎念 / 戳一戳） |
| `dist/pet_config.json` | 持久化：`ds_api_key` 等配置 |
| `dist/chat_history.jsonl` | 对话历史（重启回放，最近 40 轮） |
| `dist/sounds/pop.wav`、`ding.wav` | 音效（单击 / 收到回复） |

---

> 主人有想加的功能（比如多轮存档、自定义气泡颜色、表情贴图）随时叫喵~ 🐱
